groups:
  - name: iot_alerts
    rules:
      - alert: DeviceOffline
        expr: (time() - push_time_seconds) > 20
        labels:
          severity: critical
          # serial: "{{ $labels.serial }}"
        annotations:
          summary: "Устройство {{ $labels.serial }} не в сети"
          description: "Данные от устройства {{ $labels.serial }} не поступали более 2 часов."

      - alert: LowBattery
        expr: device_battery_level < 10
        for: 2m
        labels:
          severity: warning
          # serial: "{{ $labels.source }}"
        annotations:
          summary: "Низкий заряд батареи"
          description: "На устройстве {{ $labels.serial }} уровень заряда упал до {{ $value }}%."

      # 3. Перегрев (пример технического алерта)
      # Если температура выше 80 градусов дольше 2 минут
      - alert: DeviceOverheating
        expr: device_dryer_temp_now > 10
        for: 2m
        labels:
          severity: critical
          # serial: "{{ $labels.source }}"
        annotations:
          summary: "Перегрев устройства"
          description: "Температура на {{ $labels.serial }} поднялась до {{ $value }}°C."
